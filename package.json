{
  "private": true,
  "scripts": {
    "pretest": "lerna bootstrap",
    "test": "eslint packages --ignore-pattern 'node_modules' && lerna run test"
  },
  "jest": {
    "setupTestFrameworkScriptFile": "<rootDir>/tests/setup.js"
  },
  "devDependencies": {
    "babel-eslint": "^8.2.2",
    "chrome-launcher": "^0.1.1",
    "chrome-remote-interface": "^0.23.2",
    "eslint": "^4.19.1",
    "eslint-plugin-react": "^7.7.0",
    "jest": "^22.4.3",
    "lerna": "^2.10.0"
  }
}
